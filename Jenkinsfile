pipeline {
    agent any
    stages {
        stage("build") {
            steps {
                echo'Building the application'
                sh 'python3 app.py'

            }
        }
    }
}

